说明：
	3.2.1.上层依赖其下层，依赖关系不跨层 
		表示层不能直接访问数据访问层
		上层调用下层的结果，取决于下层的实现
	3.2.2.下一层不能调用上一层 
	3.2.3.下一层不依赖上一层
		上层的改变不会影响下一层
		下层的改变会影响上一层得到的结果
	3.2.4.在上一层中不能出现下一层的概念 
		分工明确，各司其职
		
	
	JSP ---》》servlet/action（struts2）/controller（springmvc） --》》service 上层     --》》dao   下层 --》》访问数据库